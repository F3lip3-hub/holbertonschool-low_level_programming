#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "holberton.h"
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{

	putchar(H);
	putchar(o);
	putchar(l);
	putchar(b);
	putchar(e);
	putchar(r);
	putchar(t);
	putchar(o);
	putchar(n);
	putchar(\n);
}

